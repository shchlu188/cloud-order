server:
  port: 8761 #测试order项目是默认访问端口号
spring:
  application:
    name: eureka-order-server

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eurka/
